# mongoDB_practice
This file demonstrates basic MongoDB operations including database and collection creation, CRUD operations (Create, Read, Update, Delete), and the use of query operators like $gt, $lt, $in, $and, $or, and $exists. It also includes sorting, limiting, and a simple real-world use case for practice.
